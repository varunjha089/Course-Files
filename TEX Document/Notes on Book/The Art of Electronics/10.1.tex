\documentclass[border=10pt]{standalone}
\usepackage{circuitikz}

\begin{document}

\begin{circuitikz}
    % Draw resistor from +5V
    \draw (0,3) node[anchor=south]{+5V}
          to[R=1.0k] (0,2)
          to[short] (0,1.5)
          to[short] (0,1.3) 
          to[short] (0,1)
          to[push button] (0,0) 
          to[short] (0,-1)
          node[ground]{};

    % Add switch label
    % \draw (0,1.3) -- ++(2,0) node[right]{\small SWITCH CLOSED};
    \draw (0,1.3) -- ++(2,0) node[right]{\small $\overline{\text{SWITCH    CLOSED}}$};

\end{circuitikz}

\end{document}
